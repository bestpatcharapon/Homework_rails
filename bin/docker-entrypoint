#!/bin/bash -e

# If running the rails server then load database schema and seed
if [[ "$*" == *"rails"* ]] && [[ "$*" == *"server"* ]]; then
  echo "==> Running database setup..."
  ./bin/rails db:schema:load db:seed || true
  echo "==> Database setup complete"
fi

exec "${@}"
